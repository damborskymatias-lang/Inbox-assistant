---
description: 'A inbox-prototype app.'
labels: ['app', 'react']
---

A React application for inbox-prototype. 

## Run the application

Import the app and add the following config to your workspace.jsonc:

```
bit use inbox-prototype 
```

To run the app locally, run the following:

```
bit run inbox-prototype 
```
